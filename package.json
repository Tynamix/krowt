{
  "name": "cli-krowt",
  "version": "0.2.1",
  "description": "AI coding sessions in isolated git worktrees",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Tynamix/krowt.git"
  },
  "type": "module",
  "bin": {
    "krowt": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "npm run build && vitest run",
    "typecheck": "tsc -p tsconfig.json",
    "prepack": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "smol-toml": "^1.7.1"
  }
}
